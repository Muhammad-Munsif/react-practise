import React, { useState } from 'react'

const Toggle = () => {
    const handleComp = () =>{
        const [count, setCount] = useState( )
    }
  return (
    <div>
      <button onClick={handleComp} className='px-6 py-2 rounded-lg text-white bg-indigo-500'>click it</button>
    </div>
  )
}

export default Toggle
